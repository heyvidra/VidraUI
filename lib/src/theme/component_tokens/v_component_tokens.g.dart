// GENERATED CODE — DO NOT EDIT.
//
// Regenerate with:
//   dart run scripts/generate_component_tokens.dart
//
// Registry: scripts/token_registry.dart

// coverage:ignore-file

part of 'v_component_tokens.dart';

VComponentTokens _$fromColors(VColors colors) {
  return VComponentTokens(
    appBar: VAppBarTokens.fromColors(colors),
    button: VButtonTokens.fromColors(colors),
    checkbox: VCheckboxTokens.fromColors(colors),
    chip: VChipTokens.fromColors(colors),
    menu: VMenuTokens.fromColors(colors),
    radio: VRadioTokens.fromColors(colors),
    select: VSelectTokens.fromColors(colors),
    switch_: VSwitchTokens.fromColors(colors),
    surface: VSurfaceTokens.fromColors(colors),
    dialog: VDialogTokens.fromColors(colors),
    input: VInputTokens.fromColors(colors),
    toast: VToastTokens.fromColors(colors),
    datePicker: VDatePickerTokens.fromColors(colors),
    timePicker: VTimePickerTokens.fromColors(colors),
    table: VTableTokens.fromColors(colors),
    scrollbar: VScrollbarTokens.fromColors(colors),
    segmentedControl: VSegmentedControlTokens.fromColors(colors),
    accordion: VAccordionTokens.fromColors(colors),
    autoSuggest: VAutoSuggestTokens.fromColors(colors),
    teachingTip: VTeachingTipTokens.fromColors(colors),
    navigationBar: VNavigationBarTokens.fromColors(colors),
    steps: VStepsTokens.fromColors(colors),
    timeline: VTimeLineTokens.fromColors(colors),
    divider: VDividerTokens.fromColors(colors),
    slider: VSliderTokens.fromColors(colors),
  );
}

VComponentTokens _$lerp(VComponentTokens a, VComponentTokens b, double t) {
  return VComponentTokens(
    appBar: VAppBarTokens.lerp(a.appBar, b.appBar, t),
    button: VButtonTokens.lerp(a.button, b.button, t),
    checkbox: VCheckboxTokens.lerp(a.checkbox, b.checkbox, t),
    chip: VChipTokens.lerp(a.chip, b.chip, t),
    menu: VMenuTokens.lerp(a.menu, b.menu, t),
    radio: VRadioTokens.lerp(a.radio, b.radio, t),
    select: VSelectTokens.lerp(a.select, b.select, t),
    switch_: VSwitchTokens.lerp(a.switch_, b.switch_, t),
    surface: VSurfaceTokens.lerp(a.surface, b.surface, t),
    dialog: VDialogTokens.lerp(a.dialog, b.dialog, t),
    input: VInputTokens.lerp(a.input, b.input, t),
    toast: VToastTokens.lerp(a.toast, b.toast, t),
    datePicker: VDatePickerTokens.lerp(a.datePicker, b.datePicker, t),
    timePicker: VTimePickerTokens.lerp(a.timePicker, b.timePicker, t),
    table: VTableTokens.lerp(a.table, b.table, t),
    scrollbar: VScrollbarTokens.lerp(a.scrollbar, b.scrollbar, t),
    segmentedControl: VSegmentedControlTokens.lerp(a.segmentedControl, b.segmentedControl, t),
    accordion: VAccordionTokens.lerp(a.accordion, b.accordion, t),
    autoSuggest: VAutoSuggestTokens.lerp(a.autoSuggest, b.autoSuggest, t),
    teachingTip: VTeachingTipTokens.lerp(a.teachingTip, b.teachingTip, t),
    navigationBar: VNavigationBarTokens.lerp(a.navigationBar, b.navigationBar, t),
    steps: VStepsTokens.lerp(a.steps, b.steps, t),
    timeline: VTimeLineTokens.lerp(a.timeline, b.timeline, t),
    divider: VDividerTokens.lerp(a.divider, b.divider, t),
    slider: VSliderTokens.lerp(a.slider, b.slider, t),
  );
}

VComponentTokens _$copyWith(
  VComponentTokens self, {
  VAppBarTokens? appBar,
  VButtonTokens? button,
  VCheckboxTokens? checkbox,
  VChipTokens? chip,
  VMenuTokens? menu,
  VRadioTokens? radio,
  VSelectTokens? select,
  VSwitchTokens? switch_,
  VSurfaceTokens? surface,
  VDialogTokens? dialog,
  VInputTokens? input,
  VToastTokens? toast,
  VDatePickerTokens? datePicker,
  VTimePickerTokens? timePicker,
  VTableTokens? table,
  VScrollbarTokens? scrollbar,
  VSegmentedControlTokens? segmentedControl,
  VAccordionTokens? accordion,
  VAutoSuggestTokens? autoSuggest,
  VTeachingTipTokens? teachingTip,
  VNavigationBarTokens? navigationBar,
  VStepsTokens? steps,
  VTimeLineTokens? timeline,
  VDividerTokens? divider,
  VSliderTokens? slider,
}) {
  return VComponentTokens(
    appBar: appBar ?? self.appBar,
    button: button ?? self.button,
    checkbox: checkbox ?? self.checkbox,
    chip: chip ?? self.chip,
    menu: menu ?? self.menu,
    radio: radio ?? self.radio,
    select: select ?? self.select,
    switch_: switch_ ?? self.switch_,
    surface: surface ?? self.surface,
    dialog: dialog ?? self.dialog,
    input: input ?? self.input,
    toast: toast ?? self.toast,
    datePicker: datePicker ?? self.datePicker,
    timePicker: timePicker ?? self.timePicker,
    table: table ?? self.table,
    scrollbar: scrollbar ?? self.scrollbar,
    segmentedControl: segmentedControl ?? self.segmentedControl,
    accordion: accordion ?? self.accordion,
    autoSuggest: autoSuggest ?? self.autoSuggest,
    teachingTip: teachingTip ?? self.teachingTip,
    navigationBar: navigationBar ?? self.navigationBar,
    steps: steps ?? self.steps,
    timeline: timeline ?? self.timeline,
    divider: divider ?? self.divider,
    slider: slider ?? self.slider,
  );
}
