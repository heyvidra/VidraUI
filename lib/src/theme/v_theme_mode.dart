/// Determines which theme configuration to use.
enum VThemeMode {
  /// Always use the light theme.
  light,

  /// Always use the dark theme.
  dark,

  /// Follow the system brightness setting.
  system,
}
