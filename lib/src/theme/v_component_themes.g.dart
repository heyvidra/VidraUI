// GENERATED CODE — DO NOT EDIT.
//
// Regenerate with:
//   dart run scripts/generate_component_themes.dart
//
// Registry: scripts/component_theme_registry.dart

// coverage:ignore-file

import 'package:flutter/widgets.dart';

import 'component_tokens.dart';
import 'v_theme.dart';
import 'v_token_theme.dart';

/// Overrides [VAccordionTokens] for its subtree.
class VAccordionTheme extends VComponentThemeWrapper<VAccordionTokens> {
  const VAccordionTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VAccordionTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VAccordionTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VAccordionTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VAccordionTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.accordion,
      child: child,
    );
}

/// Overrides [VAppBarTokens] for its subtree.
class VAppBarTheme extends VComponentThemeWrapper<VAppBarTokens> {
  const VAppBarTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VAppBarTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VAppBarTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VAppBarTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VAppBarTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.appBar,
      child: child,
    );
}

typedef VButtonTokensOverride = VScopedTokenOverride<VButtonTokens>;

/// Overrides [VButtonTokens] for its subtree.
class VButtonTheme extends VComponentThemeWrapper<VButtonTokens> {
  const VButtonTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VButtonTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VButtonTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VButtonTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VButtonTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.button,
      child: child,
    );
}

/// Overrides [VCheckboxTokens] for its subtree.
class VCheckboxTheme extends VComponentThemeWrapper<VCheckboxTokens> {
  const VCheckboxTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VCheckboxTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VCheckboxTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VCheckboxTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VCheckboxTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.checkbox,
      child: child,
    );
}

/// Overrides [VChipTokens] for its subtree.
class VChipTheme extends VComponentThemeWrapper<VChipTokens> {
  const VChipTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VChipTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VChipTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VChipTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VChipTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.chip,
      child: child,
    );
}

/// Overrides [VDatePickerTokens] for its subtree.
class VDatePickerTheme extends VComponentThemeWrapper<VDatePickerTokens> {
  const VDatePickerTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VDatePickerTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VDatePickerTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VDatePickerTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VDatePickerTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.datePicker,
      child: child,
    );
}

/// Overrides [VDialogTokens] for its subtree.
class VDialogTheme extends VComponentThemeWrapper<VDialogTokens> {
  const VDialogTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VDialogTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VDialogTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VDialogTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VDialogTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.dialog,
      child: child,
    );
}

/// Overrides [VDividerTokens] for its subtree.
class VDividerTheme extends VComponentThemeWrapper<VDividerTokens> {
  const VDividerTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VDividerTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VDividerTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VDividerTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VDividerTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.divider,
      child: child,
    );
}

/// Overrides [VInputTokens] for its subtree.
class VInputTheme extends VComponentThemeWrapper<VInputTokens> {
  const VInputTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VInputTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VInputTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VInputTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VInputTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.input,
      child: child,
    );
}

/// Overrides [VMenuTokens] for its subtree.
class VMenuTheme extends VComponentThemeWrapper<VMenuTokens> {
  const VMenuTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VMenuTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VMenuTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VMenuTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VMenuTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.menu,
      child: child,
    );
}

/// Overrides [VNavigationBarTokens] for its subtree.
class VNavigationBarTheme extends VComponentThemeWrapper<VNavigationBarTokens> {
  const VNavigationBarTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VNavigationBarTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VNavigationBarTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VNavigationBarTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VNavigationBarTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.navigationBar,
      child: child,
    );
}

/// Overrides [VRadioTokens] for its subtree.
class VRadioTheme extends VComponentThemeWrapper<VRadioTokens> {
  const VRadioTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VRadioTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VRadioTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VRadioTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VRadioTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.radio,
      child: child,
    );
}

/// Overrides [VSegmentedControlTokens] for its subtree.
class VSegmentedControlTheme extends VComponentThemeWrapper<VSegmentedControlTokens> {
  const VSegmentedControlTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VSegmentedControlTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VSegmentedControlTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VSegmentedControlTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VSegmentedControlTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.segmentedControl,
      child: child,
    );
}

/// Overrides [VSelectTokens] for its subtree.
class VSelectTheme extends VComponentThemeWrapper<VSelectTokens> {
  const VSelectTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VSelectTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VSelectTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VSelectTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VSelectTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.select,
      child: child,
    );
}

/// Overrides [VStepsTokens] for its subtree.
class VStepsTheme extends VComponentThemeWrapper<VStepsTokens> {
  const VStepsTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VStepsTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VStepsTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VStepsTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VStepsTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.steps,
      child: child,
    );
}

/// Overrides [VSurfaceTokens] for its subtree.
class VSurfaceTheme extends VComponentThemeWrapper<VSurfaceTokens> {
  const VSurfaceTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VSurfaceTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VSurfaceTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VSurfaceTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VSurfaceTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.surface,
      child: child,
    );
}

/// Overrides [VSwitchTokens] for its subtree.
class VSwitchTheme extends VComponentThemeWrapper<VSwitchTokens> {
  const VSwitchTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VSwitchTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VSwitchTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VSwitchTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VSwitchTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.switch_,
      child: child,
    );
}

/// Overrides [VTableTokens] for its subtree.
class VTableTheme extends VComponentThemeWrapper<VTableTokens> {
  const VTableTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VTableTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VTableTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VTableTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VTableTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.table,
      child: child,
    );
}

/// Overrides [VTimePickerTokens] for its subtree.
class VTimePickerTheme extends VComponentThemeWrapper<VTimePickerTokens> {
  const VTimePickerTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VTimePickerTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VTimePickerTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VTimePickerTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VTimePickerTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.timePicker,
      child: child,
    );
}

/// Overrides [VTimeLineTokens] for its subtree.
class VTimeLineTheme extends VComponentThemeWrapper<VTimeLineTokens> {
  const VTimeLineTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VTimeLineTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VTimeLineTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VTimeLineTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VTimeLineTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.timeline,
      child: child,
    );
}

/// Overrides [VToastTokens] for its subtree.
class VToastTheme extends VComponentThemeWrapper<VToastTokens> {
  const VToastTheme({
    super.key,
    required super.data,
    required super.child,
  });

  static VToastTokens? of(BuildContext context) =>
    VComponentThemeWrapper.ofToken<VToastTokens>(context);

  static Widget override({
    Key? key,
    required VScopedTokenOverride<VToastTokens> data,
    required Widget child,
  }) =>
    VTokenTheme.override<VToastTokens>(
      key: key,
      data: data,
      fallback: (theme) => theme.components.toast,
      child: child,
    );
}

