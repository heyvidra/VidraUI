/// Screen position for toast messages.
enum VToastPosition {
  bottom,
  top,
  center,
}

/// Stack mode for toast messages.
enum VToastStackMode {
  /// Replace existing toasts with new ones.
  replace,
  /// Stack new toasts above existing ones.
  stack,
}